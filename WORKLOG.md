# Worklog

## 2026-04-22

- Established the repo skeleton around strict `Shell`, `Mind`, and `Memory` boundaries.
- Defined root doctrine docs for architecture, art direction, memory, commands, modules, portrait anatomy, and roadmap.
- Added starter config, schemas, content registries, theme data, module packs, portrait pack parts, and example payloads.
- Chose a lean first slice centered on visible presence, persistence, and one symbolic memory reaction.
- Extended the shell model into a capability-aware procedural cell-rendering engine with explicit quality tiers and a multi-pass frame loop.
- Added dedicated notes, schemas, and starter registries for ambient behavior, micro-motion, distortion presets, scene modeling, and graceful terminal degradation.
- Added a no-dependency Python reference runtime that proves the `pure-text` tier with live shell rendering, persistent state, memory promotion, one visible relic unlock, companion motion, and diff-oriented redraw.
- Added explicit command parsing, scene-model and render-state JSON dump paths, operational `dream` and `forget memory` flows, state normalization for runtime migrations, and a stdlib test suite covering the first slice.
- Switched the reference runtime to module-driven command and panel registration with mode-aware availability, dynamic help output, visible panel filtering in render-state snapshots, and stronger dream/recovery shell presentation.
- Added tier-aware panel framing, manifest-driven companion visibility and motion, cadence-gated ambient behavior, loaded-mode enforcement from module packs, ISO render-state timestamps, and graceful fallback from rich Unicode to pure text when the terminal encoding cannot safely carry richer glyphs.
- Added a starter work pack that loads `building` and `researching`, plus a second memory-promotion path that turns project commitments into gallery and timeline marks.
- Added artifact-specific shell renderers so timeline markers leave seam ticks, orbit fragments appear near the portrait, and `gallery` reports visual binding targets with source memory IDs.
- Added persistent shell surface focus so `gallery` becomes a visible panel state, and strengthened `building` plus `researching` through portrait manifest updates and restrained state-layer overlays.
- Added selected memory and artifact navigation with `next` or `prev` behavior, plus mode-aware command hints so the shell teaches gallery and memory browsing when those surfaces are active.
- Added an optional no-build browser preview that reuses the same reference runtime, exposes a serialized cell-grid frame payload, accepts live commands over a tiny local server, and updates the browser surface with row-level diffs instead of a separate web-only implementation.
- Added the Slice 2 correction loop: `inspect memory <id>` surfaces full body, bindings, and revision count; `revise memory <id> <text>` rewrites summary, re-derives tags, keeps existing artifacts, and creates new ones when a new tag category appears; `dream promote [draft_id]` turns a dream draft into a durable `dream.synthesis` memory and the second promotion activates the `evo.dream-shard` orbit fragment. Memory surfacing now queues a `distortion.relic-phase` resurfacing event so the shell shows the memory becoming visible, and command hints reflect `inspect memory` plus `dream promote` when drafts exist.
- Added Vercel deploy surface: `api/frame.py` and `api/command.py` serverless handlers, `DREAMER2_STATE_DIR` env hook for /tmp state, pinned `@vercel/python@4.3.0`, linked GitHub repo `syfpsy/dreamer2`, deployed live at `https://dreamer2.vercel.app`, verified POSTs unlock the Archive Lantern in production.
- Added the DeepSeek companion provider at `dreamer2_ref/providers.py`: stdlib-urllib `DeepSeekProvider` with OpenAI-compatible `/chat/completions`, env-gated `load_provider()`, `build_system_prompt()` assembling profile + mode + recent memories + visible artifacts, cached per-app. Wired into the `send` flow after memory extraction with companion speaker prefix `aster>` and a `_split_for_log()` helper. Hardened `.gitignore` (`.env`, `.env.*`, `!.env.example`), added `.env.example`, stored the key only in `.env.local` and Vercel env store. Two new provider tests keep coverage honest.
- Extended doctrine for world generation: authored `WORLD_GENERATION.md` (invisible-first nine-stage pipeline, scene equation, semantic cells, invisible world fields, determinism streams, memory-in-world), `PLACE_GRAMMAR.md` (18 archetypes, 13 layout idioms, 12 materials, 15 motifs, palette roles, silence ratios), `ATMOSPHERE.md` (ambient fields, weather systems, distortion discipline, 11-tier motion hierarchy, timing doctrine), `COMPANION_ORGANISM.md` (identity DNA, lineages, shrine/walker/projection/embedded/remote-presence composition modes, companion objects, mutation rules), `ANIMATION_REGISTRY.md` (modular behavior families and binding pass), `GLYPH_LANGUAGE.md` (shared family system), `PACK_SYSTEM.md` (composable packs for every content class), `WORLDGEN.md` (quick-map index), `SLICE_3_ATMOSPHERE_ENGINE.md` (first vertical slice to prove the world soul). Extended `ARCHITECTURE.md` with a World layer plus expanded interface boundaries, `ROADMAP.md` with Slice 3 Atmosphere Engine before the pack slice, `MEMORY_MODEL.md` with memory-echo-in-world semantics, `PLAN.md` with the invisible-first non-negotiable.
- Added JSON schemas under `specs/schemas/`: `world/semantic-cell`, `world/world-field`, `world/scene-equation`, `world/place-archetype`, `world/layout-idiom`, `world/scar-package`, `world/focal-object`, `world/weather-system`, `animation/behavior-module`, `style/motif`, `style/palette-doctrine`, `style/material`, `portrait/companion-dna`, `portrait/lineage`, `portrait/composition-mode`.
- Seeded the Signal Chapel reference pack end-to-end under `packs/`: `world/archetypes/signal-chapel/place.json`, `world/layouts/ring-sanctum.json`, `world/focals/broken-halo-terminal.json`, `world/weather/slow-signal-dust.json`, `world/scars/failed-memory-extraction.json`, `world/fields/core.fields.json`, three materials (`shrine-plating`, `cable-veined-stone`, `stitched-membrane`), `style/motifs/core.motifs.json` (segmented-ring, stepped-halo, vow-ring, prayer-bracket, seam-stitch), `style/palettes/sacred-machine-default.json`, six animation behaviors (`breath-offset`, `core-pulse`, `anchor-pulse`, `flow-field-drift`, `ghost-trail`, `relic-phase`), `companions/composition-modes/shrine.json` and `walker.json`, and the `world/scene-equations/signal-chapel-reverent-instability.json` preset that ties them together.
- No runtime code was rewritten; the world-gen surface is fully declarative in `packs/` plus schemas in `specs/schemas/`. Fixed one pre-existing non-deterministic tier test in `tests/test_reference_runtime.py` by passing `tier_override="pure-text"`. Full suite: 28 passing.
- Implemented the Slice 3 Atmosphere Engine as a separate `dreamer2_ref/worldgen/` package: `registry.py` (pack loader with pragmatic structural validation and soft compatibility lists), `scene_equation.py` (SceneEquation dataclass + from_file loader), `cells.py` (SemanticCell, SceneGraph, RenderedCell, AnimationBinding, GLYPH_FAMILIES/PALETTE_ROLES), `streams.py` (deterministic seeded streams via hashlib), `skeleton.py` (ring-sanctum topology instantiator + single-hall fallback), `fields.py` (scalar field seeding with radial falloff and region assignment), `biome.py` (material picking, glyph-family choice within material allowance, palette role mapping, silence ratio), `focal.py` (focal installs a 3x3 altar at center with dominantMotif and applies sceneWriteBack effects), `scars.py` (memory-tag-conditional scar application marking the focal ring and traffic lane), `atmosphere.py` (weather seeding with axis bias and focal-halo alignment), `integration.py` (composition mode selection with fallback chain traversal + biome-allowed ordered fallback), `animation.py` (behavior binding with biome compatibility + fallback resolution), `synthesis.py` (within-family glyph pools with structural-by-type pools, scarred cells tilting toward decay, palette role to ANSI map), `pipeline.py` (orchestrator that runs the nine conceptual stages in order), `cli.py` + `__main__.py` (python -m dreamer2_ref.worldgen with --equation/--seed/--memory-tag/--colorize).
- Wired `soul place` into `app.py`: runs the worldgen pipeline lazily (registry cached on first call), loads `packs/world/scene-equations/signal-chapel-reverent-instability.json`, derives memory_tags from durable memories (recovery category triggers `failed-extraction`), renders the scene into the transmission log with a new `scene>` speaker and a `shell>` header summarizing biome/layout/focal/weather/mode/scars, and queues a `distortion.relic-phase` resurfacing event.
- Full suite: 41 passing (28 original + 12 worldgen + 1 soul-place integration). New tests cover: registry loads Signal Chapel chain; scene reproducibility (same equation → identical rendered text); seed change shifts rendering; scar absent without memory tag; scar applied with matching memory tag; focal claims center-of-mass altar with dominantMotif; shrine mode applied on compatible biome; fallback to walker when biome disallows shrine; preferred-missing falls to first allowed mode in archetype order; rendered glyphs obey declared cell glyph family; bindings recorded on scene; soul place writes expected header and scene lines.

Future entries should stay short and factual.
