# Worklog

## 2026-04-22

- Established the repo skeleton around strict `Shell`, `Mind`, and `Memory` boundaries.
- Defined root doctrine docs for architecture, art direction, memory, commands, modules, portrait anatomy, and roadmap.
- Added starter config, schemas, content registries, theme data, module packs, portrait pack parts, and example payloads.
- Chose a lean first slice centered on visible presence, persistence, and one symbolic memory reaction.
- Extended the shell model into a capability-aware procedural cell-rendering engine with explicit quality tiers and a multi-pass frame loop.
- Added dedicated notes, schemas, and starter registries for ambient behavior, micro-motion, distortion presets, scene modeling, and graceful terminal degradation.
- Added a no-dependency Python reference runtime that proves the `pure-text` tier with live shell rendering, persistent state, memory promotion, one visible relic unlock, companion motion, and diff-oriented redraw.
- Added explicit command parsing, scene-model and render-state JSON dump paths, operational `dream` and `forget memory` flows, state normalization for runtime migrations, and a stdlib test suite covering the first slice.
- Switched the reference runtime to module-driven command and panel registration with mode-aware availability, dynamic help output, visible panel filtering in render-state snapshots, and stronger dream/recovery shell presentation.
- Added tier-aware panel framing, manifest-driven companion visibility and motion, cadence-gated ambient behavior, loaded-mode enforcement from module packs, ISO render-state timestamps, and graceful fallback from rich Unicode to pure text when the terminal encoding cannot safely carry richer glyphs.
- Added a starter work pack that loads `building` and `researching`, plus a second memory-promotion path that turns project commitments into gallery and timeline marks.
- Added artifact-specific shell renderers so timeline markers leave seam ticks, orbit fragments appear near the portrait, and `gallery` reports visual binding targets with source memory IDs.
- Added persistent shell surface focus so `gallery` becomes a visible panel state, and strengthened `building` plus `researching` through portrait manifest updates and restrained state-layer overlays.
- Added selected memory and artifact navigation with `next` or `prev` behavior, plus mode-aware command hints so the shell teaches gallery and memory browsing when those surfaces are active.
- Added an optional no-build browser preview that reuses the same reference runtime, exposes a serialized cell-grid frame payload, accepts live commands over a tiny local server, and updates the browser surface with row-level diffs instead of a separate web-only implementation.
- Added the Slice 2 correction loop: `inspect memory <id>` surfaces full body, bindings, and revision count; `revise memory <id> <text>` rewrites summary, re-derives tags, keeps existing artifacts, and creates new ones when a new tag category appears; `dream promote [draft_id]` turns a dream draft into a durable `dream.synthesis` memory and the second promotion activates the `evo.dream-shard` orbit fragment. Memory surfacing now queues a `distortion.relic-phase` resurfacing event so the shell shows the memory becoming visible, and command hints reflect `inspect memory` plus `dream promote` when drafts exist.

Future entries should stay short and factual.
